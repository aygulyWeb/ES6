// 2)  Дан массив. Запишите последний элемент этого массива в переменную elem1,
//  а предпоследний - в переменную elem2. 


let arr = [2, 5, 4, 5, 8, 7, 9, 4, 5, 6,];

arr.reverse();

let [elem1, elem2] = arr;

console.log(`${elem1}\n${elem2}`)

